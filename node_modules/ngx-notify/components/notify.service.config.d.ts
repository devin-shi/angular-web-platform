import { Options } from './interfaces/options.type';
import { HolderOptions } from './interfaces/holder.options';
export declare class NotifyServiceConfig {
    /**
     * 容器
     *
     * @type {HTMLElement}
     * @default `document.body`
     */
    container?: HTMLElement;
    options?: HolderOptions;
    notify?: Options;
}
