import { OnInit, OnDestroy, Injector } from '@angular/core';
import { HolderOptions } from './interfaces/holder.options';
import { Notify } from './interfaces/notify.type';
export declare class NotifyHolderComponent implements OnInit, OnDestroy {
    notifies: Notify[];
    options: HolderOptions;
    styles: any;
    private listener;
    constructor(injector: Injector);
    ngOnInit(): void;
    private defaultBehavior(value);
    private getEmit(notify, to);
    private remove(id);
    private add(item);
    ngOnDestroy(): void;
}
