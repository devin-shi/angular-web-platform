import { OnDestroy, OnInit, NgZone } from '@angular/core';
import { DomSanitizer, SafeHtml } from '@angular/platform-browser';
import { Notify } from './interfaces/notify.type';
import { NotifyService } from './notify.service';
export declare class NotifyComponent implements OnInit, OnDestroy {
    private sanitizer;
    private zone;
    private notifyService;
    item: Notify;
    html: SafeHtml;
    animate: string;
    classes: any;
    constructor(sanitizer: DomSanitizer, zone: NgZone, notifyService: NotifyService);
    ngOnInit(): void;
    private stopTime;
    private timer;
    private steps;
    private speed;
    private start;
    private count;
    private diff;
    private startTimeout();
    progressWidth: number;
    private instance;
    onEnter(): void;
    onLeave(): void;
    onClick($e: MouseEvent): void;
    private remove();
    ngOnDestroy(): void;
}
