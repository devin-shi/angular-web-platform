import { Observable } from 'rxjs/Observable';
import { NzModalService, ModalOptionsForService } from 'ng-zorro-antd';
/**
 * 对话框辅助类
 */
export declare class ModalHelper {
    private srv;
    private zIndex;
    constructor(srv: NzModalService);
    /**
     * 打开对话框
     * this.modalHelper.open(FormEditComponent, { i }).subscribe(res => this.load());
     *
     * 对于组件的成功&关闭的处理说明：
     * 成功：
     *  this.NzModalRef.close(data);
     *  this.NzModalRef.close();
     *
     * 关闭：
     * this.NzModalRef.destroy();
     *
     * @param comp 组件
     * @param params 组件参数
     * @param size 大小；例如：lg、600，默认：lg
     * @param options 对话框 `ModalOptionsForService` 参数
     */
    open(comp: any, params?: any, size?: 'sm' | 'lg' | '' | number, options?: ModalOptionsForService): Observable<any>;
    /**
     * 静态框，点击蒙层不允许关闭
     */
    static(comp: any, params?: any, size?: 'sm' | 'lg' | '' | number, options?: any): Observable<any>;
}
