import { ElementRef, OnDestroy, OnChanges, SimpleChanges, ChangeDetectorRef } from '@angular/core';
export declare class G2MiniBarComponent implements OnDestroy, OnChanges {
    private cd;
    color: string;
    height: any;
    private _height;
    borderWidth: any;
    private _borderWidth;
    padding: number[];
    data: Array<{
        x: number;
        y: number;
        [key: string]: any;
    }>;
    node: ElementRef;
    chart: any;
    constructor(cd: ChangeDetectorRef);
    install(): void;
    uninstall(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
}
