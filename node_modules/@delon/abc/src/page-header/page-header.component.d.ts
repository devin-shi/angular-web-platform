import { TemplateRef, OnInit, OnChanges, SimpleChange, SimpleChanges, ElementRef, AfterViewInit, Renderer2 } from '@angular/core';
import { Router } from '@angular/router';
import { MenuService, AlainI18NService } from '@delon/theme';
import { AdPageHeaderConfig } from './page-header.config';
export declare class PageHeaderComponent implements OnInit, OnChanges, AfterViewInit {
    private renderer;
    private route;
    private menuSrv;
    private i18nSrv;
    conTpl: ElementRef;
    title: string;
    home: string;
    home_link: string;
    home_i18n: string;
    /**
     * 自动生成导航，以当前路由从主菜单中定位
     */
    autoBreadcrumb: any;
    private _autoBreadcrumb;
    paths: any[];
    breadcrumb: TemplateRef<any>;
    logo: TemplateRef<any>;
    action: TemplateRef<any>;
    content: TemplateRef<any>;
    extra: TemplateRef<any>;
    tab: TemplateRef<any>;
    constructor(cog: AdPageHeaderConfig, renderer: Renderer2, route: Router, menuSrv: MenuService, i18nSrv: AlainI18NService);
    genBreadcrumb(): void;
    checkContent(): void;
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnChanges(changes: {
        [P in keyof this]?: SimpleChange;
    } & SimpleChanges): void;
}
