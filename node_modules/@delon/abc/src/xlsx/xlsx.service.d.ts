import { HttpClient } from '@angular/common/http';
import { LazyService } from '@delon/util';
import { XlsxExportOptions, XlsxConfig } from './interface';
export declare class XlsxService {
    private config;
    private http;
    private lazy;
    constructor(config: XlsxConfig, http: HttpClient, lazy: LazyService);
    private init();
    private read(wb);
    /** 导入Excel并输出JSON，支持 `<input type="file">`、URL 形式 */
    import(fileOrUrl: File | string): Promise<{
        [key: string]: any[][];
    }>;
    /** 导出 */
    export(options: XlsxExportOptions): Promise<void>;
}
