import { ElementRef, Renderer2, OnInit, OnDestroy, ChangeDetectorRef, EventEmitter } from '@angular/core';
import { Router } from '@angular/router';
import { LocationStrategy } from '@angular/common';
import { MenuService, SettingsService, Menu } from '@delon/theme';
import { Nav } from './interface';
export declare class SidebarNavComponent implements OnInit, OnDestroy {
    private menuSrv;
    settings: SettingsService;
    private router;
    private locationStrategy;
    private render;
    private cd;
    private doc;
    private rootEl;
    /** @inner */
    floatingEl: HTMLDivElement;
    private bodyEl;
    list: Nav[];
    private change$;
    autoCloseUnderPad: boolean;
    select: EventEmitter<Menu>;
    constructor(menuSrv: MenuService, settings: SettingsService, router: Router, locationStrategy: LocationStrategy, render: Renderer2, cd: ChangeDetectorRef, doc: any, el: ElementRef);
    ngOnInit(): void;
    private floatingAreaClickHandle(e);
    clearFloatingContainer(): void;
    genFloatingContainer(): void;
    private genSubNode(linkNode, item);
    private hideAll();
    private calPos(linkNode, node);
    showSubMenu(e: MouseEvent, item: Nav): void;
    onSelect(item: Menu): void;
    toggleOpen(item: Nav): void;
    onClick(): void;
    ngOnDestroy(): void;
    private route$;
    private installUnderPad();
    private underPad();
}
