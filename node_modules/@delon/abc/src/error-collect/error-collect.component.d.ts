import { OnInit, OnDestroy, ElementRef, Renderer2, ChangeDetectorRef } from '@angular/core';
import { AdErrorCollectConfig } from './error-collect.config';
/**
 * 错误消息采集器
 * PS：虽然此法并不好看，但对响应式表单&模板表单有很好的效果。
 */
export declare class ErrorCollectComponent implements OnInit, OnDestroy {
    private el;
    private renderer;
    private cd;
    private doc;
    private $time;
    private formEl;
    freq: any;
    private _freq;
    offsetTop: any;
    private _offsetTop;
    _hiden: boolean;
    count: number;
    constructor(cog: AdErrorCollectConfig, el: ElementRef, renderer: Renderer2, cd: ChangeDetectorRef, doc: any);
    private readonly errEls;
    private update();
    _click(): boolean;
    private install();
    private uninstall();
    private findParent(el, selector);
    ngOnInit(): void;
    ngOnDestroy(): void;
}
